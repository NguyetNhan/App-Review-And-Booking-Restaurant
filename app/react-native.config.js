module.exports = {
        dependency: {
                platforms: {
                        ios: {},
                        android: {}, // projects are grouped into "platforms"
                },
                assets: ['./src/assets/fonts'],  // stays the same
        },
};