import {
} from '../actions/types';

const Index = (state = null, action) => {
        switch (action.type) {

                default:
                        return state;
        }
};

export default Index;